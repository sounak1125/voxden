<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  These entitlements only take effect once the app is actually signed; the
  current unsigned build ships them without them being applied.
  allow-jit: Electron/V8 compiles JavaScript at runtime.
  disable-library-validation + allow-unsigned-executable-memory: the bundled
  Python speech sidecar loads its own unsigned native libraries.
  device.audio-input: microphone access for dictation.
  automation.apple-events: reserved for future media control (pausing playback).
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
