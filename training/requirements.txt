# Training only. Install a CUDA PyTorch wheel FIRST; do not alter the app env.
# Tested package contracts are recorded in README.md, not a large-v3 guarantee.
torch>=2.6,<3
transformers==4.57.6
peft==0.18.0
accelerate==1.12.0
ctranslate2==4.8.1
faster-whisper==1.2.1
numpy>=1.26,<3
soundfile>=0.12,<1
# record.py only. Microphone capture for the read-speech regression set;
# nothing else in the pipeline imports it, and it is loaded lazily so the
# manifest tooling runs without it.
sounddevice>=0.4,<1
scipy>=1.11,<2
